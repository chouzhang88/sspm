========================================================================
    console application：crop  
========================================================================

this application read data from serial remote sensing images in .tif format.
the sequence database will be constructed by applying pixels-group clustering.